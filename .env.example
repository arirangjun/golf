# PostgreSQL (로컬/배포)
# Neon 예: postgresql://user:password@host/db?sslmode=require
DATABASE_URL="postgresql://user:password@localhost:5432/golf?schema=public"

# JWT 서명 키 (배포 시 반드시 랜덤 문자열로 변경)
JWT_SECRET="change-this-to-a-secure-random-string-in-production"
